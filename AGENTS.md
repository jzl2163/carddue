# CardDue 开发约定

继续现有 Rust/Svelte 实现，保留五种通知渠道、自定义模板和动态 ICS；不得通过删除需求或跳过失败测试获得通过结果。
账期以账单所属年月为稳定身份。修改日期不得制造重复日历身份；标记已还款必须取消相关待发任务。
任何涉及卡片、账期、渠道、模板、订阅和投递记录的接口都必须验证用户归属。保留 CSRF、Origin、凭据加密、模板限制和出站地址校验。
测试只使用独立数据库与模拟目的端。没有用户配置的测试目的端，不发送真实消息。
优先运行 `sh scripts/verify-docker.sh`。对本项目的 Oracle 工作，依赖与构建缓存只放服务器，不在本机安装。
验证记录必须区分代码存在、自动测试通过和真实设备通过。更新 HANDOFF.md 和 docs/verification.md，不将候选版本标成稳定 V1。
