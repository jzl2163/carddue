# 安全边界

CardDue 只记录主动录入的卡片及日期信息，不要求银行密码、CVV 或完整卡号。
私密 ICS URL、会话 Cookie、推送渠道凭据、.env、数据库备份均应作为秘密保存。不要将其提交到仓库或公开日志。

应用包含用户归属检查、CSRF/Origin 检查、会话管理、凭据认证加密、模板限制和出站地址校验。相关自动用例不代表全面安全审计。
部署时使用 HTTPS，将数据库留在私有网络，默认关闭公开注册。不要把允许私网目的端的测试配置直接用于公网服务。
泄露日历链接时轮换订阅；渠道凭据泄露时先在提供商撤销再更新配置。数据库恢复需要保留原应用密钥。
报告漏洞时提供最小复现并去除真实账户及凭据。发布前仍需完成验收清单中的安全、并发及恢复检查。
